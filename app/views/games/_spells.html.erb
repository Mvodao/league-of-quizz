
 <div class="spells row" data-controller="spell">
 <span class= "title">Choix du personnage</span>
    <% @spells.each do |spell| %>
      <div class="col-4">
        <%= render "spell", spell: spell %>
      </div>
    <% end %>
    <<%= link_to "Lancer la partie", game_path(@game.id), class: "btn btn-danger mt-2" %>
  </div>
